name: PR comment action

on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - edited
      - commented

jobs:
  your-job:
    if: github.event.comment.body == '/bang'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run your action
        run: echo "Action triggered by '/bang' comment on pull request!"
